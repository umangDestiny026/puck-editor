import { Routes } from '@angular/router';
import { ReactHostComponent } from './react-host/react-host.component';

export const routes: Routes = [
  {
    path: 'react',
    component: ReactHostComponent
  }
];
